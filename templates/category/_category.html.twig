<div class="card my-2" style="width: 18rem;">
    <div class="card-body text-center">
      <h2 class="card-title light">{{ loop.index }} - {{ category.name }} </h2>
      <a class="btn btn-primary custom-btn" href="{{ path('category_show', {name: category.name}) }}">Voir les séries.</a>
    </div>
</div>